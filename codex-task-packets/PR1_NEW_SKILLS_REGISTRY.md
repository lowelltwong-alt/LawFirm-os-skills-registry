# PR1 — Create LawFirm-os-skills-registry

Apply this repo as a new GitHub repository.

Acceptance:

```bash
python -m pip install -e ".[dev]"
python -m pytest
python -m lawfirm_os_skills_registry console
```

Do not touch the three existing repos in this PR.
